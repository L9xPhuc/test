specVersion: "v1beta"
serviceId: "web project"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "web project-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
